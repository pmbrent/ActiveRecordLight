class Relation

  #store info from WHERE queries; perform them only when e.g. an enumerable is called
  #allow for chaining of WHERE's by adding to the stored query
  #may want to redefine method_missing, overwrite each, etc.

end
